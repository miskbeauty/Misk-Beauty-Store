---
description: Push changes to GitHub
---
// turbo-all
1. Add all changes to git
```powershell
git add .
```
2. Commit changes
```powershell
git commit -m "Fix storefront 500 error, secure admin API, and update dashboard auth headers"
```
3. Push to main branch
```powershell
git push origin main
```
